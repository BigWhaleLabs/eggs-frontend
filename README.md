# Frontend code for $eggs

This frontend is to be used for landing page for $seggs.

## Local launch

1. Install dependencies with `yarn`
2. Copy `.env.example` to `.env` and fill in the required values
3. Run the server with `yarn start`
